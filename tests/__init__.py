"""
Test package for BridgeNLP.

Contains tests for core functionality, adapters, and multimodal capabilities.
"""

# We'll use test discovery instead of explicit imports to avoid circular dependencies
# Tests will still be discoverable by pytest without explicit imports
